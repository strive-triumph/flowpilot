services:
  flowpilot:
    build: .
    ports:
      - "127.0.0.1:4317:4317"
    environment:
      FLOWPILOT_HOST: 0.0.0.0
      FLOWPILOT_PORT: 4317
    volumes:
      - flowpilot-data:/app/data
    restart: unless-stopped

volumes:
  flowpilot-data:
